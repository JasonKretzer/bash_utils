alias allchecks='~/scripts/run_all_fe_be_checks.sh'
alias allchecksfull='allchecks && becheckfull'
alias be='bundle exec'
alias becheck='git ls-files -m | xargs ls -1 2>/dev/null | grep \\.rb$ | xargs rubocop'
alias becheckfull='becheck && docker compose run web bundle exec rspec'
alias binsetup='bin/setup && bin/populate-production-db'
alias docbash='docker compose exec web bash'
alias docclean='docker volume prune -a -f && docker image prune -a -f && docker system prune -a --volumes -f'
alias doccon='docker compose exec web bin/rails console'
alias doclog='docker compose logs -f web'
alias docmig='docker compose exec web bin/rails db:migrate'
alias docpry='docker attach reforge-web-1'
alias fecheck='yarn lint && yarn test && yarn check-types'
alias gf='git fetch'
alias gs='git status'
alias la='ls -la'
alias ls='ls --color'
alias prodcon='heroku run rails c -a reforge-production'
alias rebasemain='git fetch origin main:main && git rebase main'
alias reloadbash='source ~/.bash_profile && source ~/.bashrc && echo "Reloaded bash profile"'
alias squash2='git rebase -i HEAD~2'
alias squash3='git rebase -i HEAD~3'
alias squash4='git rebase -i HEAD~4'
alias storybook='docker compose exec webpack yarn storybook'
